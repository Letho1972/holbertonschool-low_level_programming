#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - positif or negatif
 * Return: success
 */

int main(void)
{
	int i;

	int letter;

		for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}

		for (letter = 97; letter <= 102; letter++)
	{
		putchar(letter);
	}

		putchar('\n');

return (0);
}
