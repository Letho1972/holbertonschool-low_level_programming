#include "main.h"
#include <stdio.h>

/**
 * main - Write a program that prints the number of arguments
 * @argc: The number of command line arguments
 * @argv: An array of size argc:x
 * Return: new line
 */

int main(int argc, char *argv[]__attribute__((unused)))
		{
		printf("%d\n", argc - 1);

		return (0);
		}
